import { useEffect, useState } from "react"; import { Cat, CheckCircle2 } from "lucide-react"; import { getSecrets, SECRET_EVENT } from "../secretSystem";
const missions=[
 ["lily","Faça o lírio desabrochar"], ["cat-8","Descubra quantos carinhos o gato quer"], ["gallery-heart","Tem um coração quase invisível em uma foto"], ["golden-leaf","Pegue algo raro antes que escape"]
];
export default function CatMissions(){const [found,setFound]=useState([]);useEffect(()=>{const sync=()=>setFound(getSecrets());sync();window.addEventListener(SECRET_EVENT,sync);return()=>window.removeEventListener(SECRET_EVENT,sync)},[]);return <section className="px-6 py-20"><div className="mx-auto max-w-3xl rounded-[2rem] border border-blue-100 bg-white p-7 sm:p-9"><div className="flex items-center gap-4"><span className="rounded-2xl bg-blue-50 p-3 text-blue-700"><Cat/></span><div><p className="text-xs font-bold uppercase tracking-[.25em] text-blue-500">missões do gatinho</p><h2 className="text-2xl font-black text-slate-900">Ele sabe onde algumas coisas estão.</h2></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2">{missions.map(([id,label])=>{const done=found.includes(id);return <div key={id} className={`rounded-2xl border p-4 text-sm ${done?'border-blue-200 bg-blue-50 text-blue-800':'border-slate-100 bg-slate-50 text-slate-500'}`}>{done?<CheckCircle2 className="mb-2" size={18}/>:<Cat className="mb-2" size={18}/>} {done?label:"??? — dica: "+label.toLowerCase()}</div>})}</div></div></section>}
